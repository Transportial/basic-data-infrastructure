{
  "name": "@bdi/identity",
  "version": "0.1.0",
  "private": true,
  "description": "Keycloak OIDC access-token verification, eHerkenning SAML broker, pluggable AuthnPort.",
  "license": "EUPL-1.2",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@bdi/kernel": "workspace:*",
    "@bdi/crypto": "workspace:*"
  }
}
